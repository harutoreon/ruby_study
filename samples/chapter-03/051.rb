divisor = 10

unless divisor == 0
  puts "100を#{divisor}で割った数は#{100 / divisor}です"
end

divisor = 0

unless divisor == 0
  puts "100を#{divisor}で割った数は#{100 / divisor}です"
elsif
  puts "0で割ることはできません"
end
